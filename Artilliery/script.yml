config:
  target: https://7qwgcplup1.execute-api.eu-west-1.amazonaws.com/testcn
  phases:
    - duration: 600
      arrivalRate: 10
  # defaults:
  #   headers:
  #     x-api-key: "{{ $processEnvironment.SERVICE_API_KEY }}"
scenarios:
  - flow:
      - get:
          url: "/"